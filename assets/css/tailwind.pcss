@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    font-family: "Rubik", sans-serif;
  }
}

@layer utilities {
  .gradient-text {
    @apply inline-block text-transparent bg-gradient-to-r from-primary to-secondary-clr bg-clip-text;
  }
}
